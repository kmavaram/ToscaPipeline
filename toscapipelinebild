#!groovy
pipeline{

    agent any
    parameter{

    choice(
        choices: ['DEV', 'Delta', 'Golf', 'Alfa'], 
        description: 'Please select the Environemnt', 
        name: 'DbName')
        booleanParam(
            defaultValue: false, 
            description: 'Setup for first run', 
            name: 'FIRST_RUN')

    }
}